#include <stdio.h>
#include <stdbool.h>

int main() {
    // Boolean variables with bool type
    bool isRaining = true;  // true - 1
    bool isSunny = false;   // false - 0

    printf("Is it raining: %d\n", isRaining);

    printf("Is it sunny: %d", isSunny);

    return 0;
}
